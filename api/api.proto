syntax = "proto3";
package api;

option go_package = "gen/api";

service API {
    rpc Interests(EmptyRequest) returns (InterestsRespose);
}

message EmptyRequest {}

message InterestsRespose{
    repeated string interest = 1;
}